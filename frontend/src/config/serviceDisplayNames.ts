/**
 * User-facing service labels (API paths and env keys stay unchanged).
 */
export const QUERY_EXECUTION_SERVICE_LABEL = 'Query execution service';

/** Prefix for data rows sourced via the query-execution integration. */
export const QUERY_EXECUTION_SOURCE_PREFIX = 'Query execution';
